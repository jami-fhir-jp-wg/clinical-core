RuleSet:  toplevel_short_definition(note)
* . ^short = {note}
* . ^definition = {note}

RuleSet:  relative_short_definition(note)
* ^short = {note}
* ^definition = {note}

RuleSet:  relativeLevel1_short_definition(note)
  * ^short = {note}
  * ^definition = {note}

RuleSet:  relativeLevel2_short_definition(note)
    * ^short = {note}
    * ^definition = {note}
